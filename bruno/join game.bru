meta {
  name: join game
  type: http
  seq: 3
}

post {
  url: http://localhost:9876/api/game/join?code={{join_code}}
  body: none
  auth: inherit
}

params:query {
  code: {{join_code}}
}

vars:pre-request {
  join_code: 967698
}

settings {
  encodeUrl: true
  timeout: 0
}
