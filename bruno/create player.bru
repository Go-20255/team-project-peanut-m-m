meta {
  name: create player
  type: http
  seq: 4
}

post {
  url: http://localhost:9876/api/player
  body: formUrlEncoded
  auth: inherit
}

body:form-urlencoded {
  player_name: banana
  session_id: {{uuid}}
}

settings {
  encodeUrl: true
  timeout: 0
}
