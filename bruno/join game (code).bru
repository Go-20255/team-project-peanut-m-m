meta {
  name: join game (code)
  type: http
  seq: 5
}

post {
  url: http://localhost:9876/api/game/join?code={{join_code}}
  body: none
  auth: inherit
}

params:query {
  code: {{join_code}}
}

vars:pre-request {
  join_code: 824536
}

settings {
  encodeUrl: true
  timeout: 0
}
